[project]
name = "dgc-app-backend-demo"
version = "0.0.0"
description = "Backend for storing data for the RCPCH Digital Growth Charts app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django==5.2.6",
    "django-ninja==1.4.3",
    "psycopg[binary]==3.2.10",
]
